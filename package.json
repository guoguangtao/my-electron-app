{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "src/main/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "rebuild": "node-gyp rebuild",
    "dev": "node-gyp build && electron .",
    "rebuild-and-start": "node-gyp clean && node-gyp configure && node-gyp build && electron ."
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-addon-api": "^5.1.0"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "node-gyp": "^9.0.0"
  }
}
